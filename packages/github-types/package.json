{
  "name": "@gh-dashboard/github-types",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo 'No tests configured'",
    "lint": "echo 'No linter configured'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "deploy": "echo 'No deploy configured'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
